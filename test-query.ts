import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient(); async function main() { const all = await prisma.file.findMany(); console.log("Total files:", all.length); console.log("Starred in DB:", all.filter(f => f.isStarred).length); const starred = await prisma.file.findMany({ where: { isStarred: true } }); console.log("Prisma Starred query count:", starred.length); } main().finally(() => prisma.$disconnect());